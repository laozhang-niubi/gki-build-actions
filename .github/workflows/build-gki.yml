name: Build GKI 5.10.252
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - run: echo hi
      - name: Upload log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: log
          path: build.log
